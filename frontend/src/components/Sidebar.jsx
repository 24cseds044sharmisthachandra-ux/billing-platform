import { NavLink, useNavigate } from 'react-router-dom'; import { LayoutDashboard, Package, Boxes, Users, ShoppingCart, Ticket, LogOut, Command } from 'lucide-react';
const links = [['/', 'Overview', LayoutDashboard], ['/products', 'Products', Package], ['/stock', 'Stock control', Boxes], ['/customers', 'Customers', Users], ['/orders', 'Orders', ShoppingCart], ['/coupons', 'Coupons', Ticket]];
export default function Sidebar() { const navigate = useNavigate(); return <aside className="sidebar"><div className="brand"><span className="brand-mark"><Command size={19}/></span><span>inventory<span className="brand-accent">hub</span></span></div><div className="workspace"><span className="workspace-dot"/>Main workspace <span className="chevron">⌄</span></div><nav>{links.map(([to, label, Icon]) => <NavLink key={to} to={to} end={to === '/'} className={({ isActive }) => isActive ? 'nav-link active' : 'nav-link'}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-bottom"><div className="help-box"><strong>Need a hand?</strong><span>Read the quickstart guide</span></div><button className="logout" onClick={() => { localStorage.removeItem('inventory_token'); navigate('/login'); }}><LogOut size={17}/> Sign out</button></div></aside>; }
