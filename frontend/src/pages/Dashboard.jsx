import { useEffect, useState } from 'react'; import { Package, Users, ShoppingCart, DollarSign, AlertTriangle, ArrowUpRight } from 'lucide-react'; import api from '../services/api'; import AppShell from '../components/AppShell'; import StatCard from '../components/StatCard';
export default function Dashboard() { const [stats, setStats] = useState({}); const [orders, setOrders] = useState([]); useEffect(() => { api.get('/dashboard').then(r => setStats(r.data)).catch(() => {}); api.get('/orders').then(r => setOrders(r.data.slice(0, 5))).catch(() => {}); }, []); return <AppShell title="Good morning, Admin" eyebrow="Tuesday, September 18, 2026"><div className="welcome-line">Here’s what’s happening with your workspace today. <span>Everything is up to date.</span></div><div className="stats-grid"><StatCard label="Total products" value={stats.products ?? '—'} note="Active in catalog" icon={Package} tone="teal"/><StatCard label="Inventory value" value={stats.sales ? `$${Number(stats.sales).toLocaleString()}` : '$0'} note="Gross sales this period" icon={DollarSign} tone="blue"/><StatCard label="Customers" value={stats.customers ?? '—'} note="Across all segments" icon={Users} tone="gold"/><StatCard label="Open orders" value={stats.orders ?? '—'} note="Needs attention" icon={ShoppingCart} tone="rose"/></div><div className="dashboard-grid"><section className="panel chart-panel"><div className="panel-head"><div><h2>Sales overview</h2><p>Revenue performance this month</p></div><select><option>Last 30 days</option><option>Last 7 days</option></select></div><div className="chart"><div className="chart-y"><span>$12k</span><span>$8k</span><span>$4k</span><span>$0</span></div><div className="chart-area"><div className="grid-lines"><i/><i/><i/><i/></div><svg viewBox="0 0 700 220" preserveAspectRatio="none"><defs><linearGradient id="fill" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stopColor="#27b69b" stopOpacity=".22"/><stop offset="1" stopColor="#27b69b" stopOpacity="0"/></linearGradient></defs><path d="M0 175 C80 160 90 145 145 155 S225 105 280 130 S355 95 400 115 S475 45 520 80 S590 55 700 25 L700 220 L0 220Z" fill="url(#fill)"/><path d="M0 175 C80 160 90 145 145 155 S225 105 280 130 S355 95 400 115 S475 45 520 80 S590 55 700 25" fill="none" stroke="#22a98d" strokeWidth="3"/></svg><div className="chart-x"><span>Aug 20</span><span>Aug 27</span><span>Sep 03</span><span>Sep 10</span><span>Sep 17</span></div></div></div></section><section className="panel alert-panel"><div className="panel-head"><div><h2>Stock attention</h2><p>Items below reorder level</p></div><span className="alert-count">{stats.lowStock || 0}</span></div><div className="alert-empty"><div className="alert-icon"><AlertTriangle size={20}/></div><strong>{stats.lowStock ? 'Items need restocking' : 'All stock levels look good'}</strong><p>{stats.lowStock ? 'Review the stock control page for details.' : 'No products are currently below their reorder level.'}</p></div><a href="/stock" className="text-link">Review stock <ArrowUpRight size={15}/></a></section></div><section className="panel orders-panel"><div className="panel-head"><div><h2>Recent orders</h2><p>Your latest customer activity</p></div><a href="/orders" className="text-link">View all <ArrowUpRight size={15}/></a></div><OrderTable orders={orders}/></section></AppShell>; }
function OrderTable({ orders }) { return <div className="table-wrap"><table><thead><tr><th>Order</th><th>Customer</th><th>Date</th><th>Amount</th><th>Status</th></tr></thead><tbody>{orders.length ? orders.map(o => <tr key={o._id}><td className="strong">{o.orderNumber}</td><td>{o.customer?.name || 'Customer'}</td><td>{new Date(o.createdAt).toLocaleDateString()}</td><td className="strong">${Number(o.total || 0).toFixed(2)}</td><td><span className={`badge ${o.status}`}>{o.status}</span></td></tr>) : <tr><td colSpan="5" className="empty">No orders yet. Create your first order to see it here.</td></tr>}</tbody></table></div>; }
