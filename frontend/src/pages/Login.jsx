import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { LockKeyhole, ArrowRight } from 'lucide-react'; import { auth } from '../services/api'; import Toast from '../components/Toast';
export default function Login() { const [form, setForm] = useState({ email: 'admin@example.com', password: 'ChangeMe123!' }); const [error, setError] = useState(''); const navigate = useNavigate(); const submit = async e => { e.preventDefault(); try { const { data } = await auth.login(form); localStorage.setItem('inventory_token', data.token); navigate('/'); } catch (err) { setError(err.response?.data?.message || 'Unable to sign in'); } }; return <div className="login-page"><div className="login-art"><div className="brand light"><span className="brand-mark"><LockKeyhole size={18}/></span>inventory<span className="brand-accent">hub</span></div><div className="art-copy"><div className="eyebrow light-text">OPERATIONS, IN FOCUS</div><h1>Make every unit<br/><em>count.</em></h1><p>One calm command center for inventory, customers, and the moments that move your business forward.</p></div><div className="art-footer">© 2026 Inventory Hub <span>Secure workspace</span></div></div><div className="login-panel"><div className="login-inner"><div className="eyebrow">WELCOME BACK</div><h2>Sign in to your workspace</h2><p className="login-sub">Enter your administrator credentials to continue.</p><form onSubmit={submit}><label>Email address<input type="email" value={form.email} onChange={e => setForm({...form, email:e.target.value})} required/></label><label>Password<input type="password" value={form.password} onChange={e => setForm({...form, password:e.target.value})} required/></label><div className="form-row"><label className="check"><input type="checkbox"/> Remember me</label><a href="#reset">Forgot password?</a></div><button className="primary-btn wide">Sign in <ArrowRight size={17}/></button></form><Toast message={error} error/><div className="secure-note"><LockKeyhole size={14}/> Your connection is encrypted and secure</div></div></div></div>; }
